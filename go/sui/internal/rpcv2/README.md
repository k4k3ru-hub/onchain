# Sui RPC v2 generated code

This directory contains Go and gRPC code generated from the official
[`MystenLabs/sui-apis`](https://github.com/MystenLabs/sui-apis) protocol
definitions at commit `201981c5d5fb99c8908f99b4a853fe47511cd6e3`.

The generated package is internal so the public `sui` API does not expose
protobuf or gRPC implementation types. Regenerate all files together when the
official v2 protocol definitions change.
